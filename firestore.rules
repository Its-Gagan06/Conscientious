rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Basic allow all rule for preview environment development
    // (In production, replace with proper auth rules)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
